#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @x: integer x
 * @y: integer y
 *
 * Return: Always 0 (Sucess)
 */
int add(int x, int y)
{
int result;

result = x + y;
return (result);
}
