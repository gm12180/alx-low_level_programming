#include "main.h"
/**
 * _isdigit - a fuction that checks if input is a digit
 * @c: integer c
 *
 * Return: Return 1 if true, else return 0
 */
int _isdigit(int c)
{
if (c >= 0 && c <= 9)
{
	return (1);
}
else
	return (0);
}
